{
  "name": "web-manager",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run build && bun run --watch index.ts",
    "dev:build": "bun build src/client/index.tsx --outdir public --target browser --format esm --watch",
    "start": "bun run index.ts",
    "build": "bun build src/client/index.tsx --outdir public --target browser --format esm",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.0",
    "@tanstack/react-query": "^5.83.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
